import { PanDirective } from './pan.directive';

describe('PanDirective', () => {
  it('should create an instance', () => {
    const directive = new PanDirective();
    expect(directive).toBeTruthy();
  });
});
